# A runner of our application and assembler of different modules (layers)

This contains the main method and will launch the http server, inject business, web, frontend, data layers and bind them all together